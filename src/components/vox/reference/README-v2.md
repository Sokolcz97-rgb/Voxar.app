# Structural community V2

`community-structural-v2.css` and `community-structural-v2-fix.css` are the final visual ownership layer for the current `sv-*` community shell.

They replace the remaining runtime dependency on `community-reference-pass7.css` while keeping the component structure intact.
